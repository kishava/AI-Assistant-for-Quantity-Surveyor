---
description: Auto-commit after every completed agent task
alwaysApply: true
---

# Auto-commit on every task

After completing **any** code change, fix, or feature — no exceptions:

1. Run `commit.bat` with a short conventional message:
   ```
   commit.bat "feat: brief description of what changed"
   ```
2. Use prefixes: `feat:` (features), `fix:` (bugs), `style:` (UI/CSS), `docs:` (README), `chore:` (config/hooks).
3. If no custom message is needed, run `commit.bat` with no arguments — it auto-generates one.
4. Do **not** skip the commit. Do **not** wait for the user to ask.
5. Do **not** commit secrets (`.env`, credentials). These are gitignored.
6. One commit per logical task. If you finish multiple distinct tasks in one session, commit after each one.

The Cursor `stop` hook also commits any leftover changes when the agent session ends.
